@model IEnumerable<CTsinhvien.Models.Subjectcla>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Subjectname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Course)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Subjectname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Course)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Subjectclsid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Subjectclsid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Subjectclsid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
